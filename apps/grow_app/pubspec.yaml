name: grow_app
description: The GROW application shell.
publish_to: none
version: 0.1.0
resolution: workspace

environment:
  sdk: ^3.10.0
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.6.1
  grow_content: ^0.1.0
  grow_data: ^0.1.0
  grow_domain: ^0.1.0
  grow_flora: ^0.1.0
  grow_render: ^0.1.0
  grow_sim: ^0.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^6.0.0
