name: grow_flora
description: Procedural plant geometry. Pure Dart, so it can be tested, previewed and exported without a renderer.
publish_to: none
version: 0.1.0
resolution: workspace

environment:
  sdk: ^3.10.0

dependencies:
  grow_domain: ^0.1.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
