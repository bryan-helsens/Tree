name: grow_data
description: Persistence for GROW. A save is one atomic document.
publish_to: none
version: 0.1.0
resolution: workspace

environment:
  sdk: ^3.10.0

dependencies:
  grow_domain: ^0.1.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
